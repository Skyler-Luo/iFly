# iFly 环境变量配置模板
# 复制此文件为 .env 并填入实际值

# Django 密钥 (生产环境必须更换为新的随机密钥)
SECRET_KEY=your-secret-key-here

# 调试模式 (生产环境设为 False)
DEBUG=True

# 允许的主机 (生产环境设置具体域名)
ALLOWED_HOSTS=localhost,127.0.0.1

# 数据库配置 (可选，默认使用SQLite)
# DATABASE_URL=sqlite:///db.sqlite3

# 缓存配置 (可选，默认使用内存缓存)
# REDIS_URL=redis://127.0.0.1:6379/1
